import { ScreenStub } from "@/src/ui/ScreenStub";

export default function SettingsScreen() {
  return <ScreenStub titleKey="screens.settings.title" descriptionKey="screens.settings.description" />;
}
