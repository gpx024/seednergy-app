import { createSupabaseServerClient } from "@/lib/supabase/server";
export default async function AuditPage(){const supabase=await createSupabaseServerClient();const {data,error}=await supabase.from("cms_audit_log").select("id,action,entity_type,entity_id,details,occurred_at").order("occurred_at",{ascending:false}).limit(100);return <><p className="eyebrow">Accountability</p><h1>Audit log</h1>{error&&<p className="error">{error.message}</p>}<table className="table"><thead><tr><th>When</th><th>Action</th><th>Entity</th><th>Details</th></tr></thead><tbody>{(data??[]).map(row=><tr key={row.id}><td>{new Date(row.occurred_at).toLocaleString()}</td><td>{row.action}</td><td>{row.entity_type} {row.entity_id}</td><td><code>{JSON.stringify(row.details)}</code></td></tr>)}</tbody></table></>}
